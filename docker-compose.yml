services:
  postgres:
    image: postgres:18-alpine
    container_name: stolat-postgres
    environment:
      POSTGRES_DB: stolat
      POSTGRES_USER: stolat
      POSTGRES_PASSWORD: stolat
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  mailpit:
    image: axllent/mailpit
    container_name: stolat-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  postgres-data:
